# API Backend URL.
# - Development: trỏ tới backend (đã có proxy rewrites trong next.config.mjs)
# - Production: nếu backend ở domain khác, trỏ full URL và backend cần cấu hình CORS
#   CLIENT_URL trong .env của backend.
NEXT_PUBLIC_API_URL=http://localhost:8000/api